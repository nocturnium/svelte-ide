// The playground is the full multi-pane IDE shell (editor + AI panel + plugins).
// Client-only: it's an interactive app, not a crawlable content page.
export const ssr = false;
export const prerender = false;
