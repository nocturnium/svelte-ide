// Time Machine + Conflict Theater run on Yjs/CRDT awareness (Y.Doc + Awareness
// created at component init) — client-only. Keep out of the prerender pass.
export const ssr = false;
export const prerender = false;
