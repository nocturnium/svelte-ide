# Managed by npm — rewritten on every install; formatting it just causes churn.
package-lock.json

# Generated by semantic-release on every release; formatting would fight the generator.
CHANGELOG.md
